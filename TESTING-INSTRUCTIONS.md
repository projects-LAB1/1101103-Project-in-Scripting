# วิธีทดสอบการวิเคราะห์การนอนด้วย AI

## การแก้ไขปัญหา "Cannot read property 'averageDurationHours' of undefined"

การแก้ไขปัญหาดังกล่าวได้ดำเนินการดังนี้:

1. ปรับปรุงการจัดการค่า `undefined` ในไฟล์ `utils/sleepAI.js`:
   - เพิ่มค่าเริ่มต้น (default values) สำหรับทุกตัวแปรที่อาจเป็น undefined
   - เพิ่มการจัดการข้อผิดพลาดด้วย try/catch ในทุกฟังก์ชัน
   - เพิ่มโครงสร้างข้อมูลเริ่มต้นที่ครบถ้วนเพื่อป้องกันการเข้าถึงคุณสมบัติที่ไม่มีอยู่
   - เพิ่มคุณสมบัติ `avgDurationHours` เพื่อความเข้ากันได้กับคุณสมบัติเดิมที่ใช้ใน UI

2. ปรับปรุงฟังก์ชันใน SleepAnalyticsScreen และ SleepHomeScreen:
   - เพิ่มการตรวจสอบค่า null/undefined ก่อนการเข้าถึง
   - ใช้ optional chaining (`?.`) เพื่อป้องกันข้อผิดพลาดในการเข้าถึงคุณสมบัติย่อย
   - ใช้ค่าเริ่มต้นสำหรับทุกการแสดงผลที่อ้างอิงข้อมูลจาก AI

3. สร้างระบบทดสอบเพื่อให้เห็นว่า AI วิเคราะห์ทำงานอย่างไร:
   - หน้า SleepTestScreen สำหรับแสดงผลการวิเคราะห์แบบต่างๆ
   - เพิ่มชุดข้อมูลตัวอย่าง 3 รูปแบบ (ดี, แย่, ไม่สม่ำเสมอ)
   - เพิ่มฟังก์ชันทดสอบครบทุกฟังก์ชันของ AI

## วิธีทดสอบการแก้ไข

1. เปิดหน้าจอหลักการนอนหลับ จากนั้นกดปุ่มทดสอบ (ไอคอนหลอดทดลอง) ที่มุมขวาบน เพื่อเข้าสู่หน้าทดสอบ

2. ในหน้าทดสอบ:
   - ลองสลับไปมาระหว่างรูปแบบการนอนทั้ง 3 รูปแบบ (ดี, แย่, ไม่สม่ำเสมอ)
   - สังเกตว่าการวิเคราะห์, คำแนะนำ, และการตรวจจับปัญหาเปลี่ยนแปลงตามรูปแบบที่เลือก
   - กดปุ่ม "ทดสอบฟังก์ชันทั้งหมด" เพื่อตรวจสอบว่าทุกฟังก์ชันทำงานได้อย่างถูกต้อง

3. เปิดหน้าจอข้อมูลการนอนหลับ (SleepHomeScreen) ตรวจสอบว่า:
   - ส่วนสรุปการนอนหลับแสดงผลโดยไม่มีข้อผิดพลาด
   - ส่วนคำแนะนำประจำวันแสดงผลโดยไม่มีข้อผิดพลาด
   - กดดูการวิเคราะห์ทั้งหมดเพื่อเข้าสู่หน้า SleepAnalyticsScreen

4. ในหน้าวิเคราะห์การนอน (SleepAnalyticsScreen) ตรวจสอบว่า:
   - ส่วนวิเคราะห์การนอนด้วย AI แสดงผลโดยไม่มีข้อผิดพลาด
   - ส่วนกราฟและสถิติแสดงผลโดยไม่มีข้อผิดพลาด
   - ลองสลับช่วงเวลาระหว่าง สัปดาห์/เดือน/ปี ดูว่ามีข้อผิดพลาดหรือไม่

## ข้อมูลเพิ่มเติม

การแก้ไขนี้รองรับทั้ง:
1. กรณีที่ยังไม่มีข้อมูลการนอน (แสดงข้อความแนะนำให้เพิ่มข้อมูล)
2. กรณีที่มีข้อมูลน้อย (แสดงการวิเคราะห์เบื้องต้นพร้อมคำแนะนำให้เพิ่มข้อมูล)
3. กรณีที่มีข้อมูลเพียงพอ (แสดงการวิเคราะห์โดยละเอียด)

สำหรับการทดสอบกรณีพิเศษ สามารถไปที่หน้าทดสอบและทดลองใช้งานกับข้อมูลทดสอบทั้ง 3 รูปแบบได้ 